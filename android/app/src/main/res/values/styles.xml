<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- android:statusBarColor / android:navigationBarColor sont depreciees a partir
         d'Android 15 (API 35) : l'edge to edge est impose par l'OS et ces attributs
         sont ignores de facon incoherente selon les appareils. La couleur de fond
         est desormais geree cote web (MobileTopBar/MobileBottomNav, fond bleu plus
         padding sur les safe area insets) via StatusBar.setOverlaysWebView overlay true. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">@color/colorPrimary</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

</resources>
